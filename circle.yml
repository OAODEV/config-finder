machine:
  services:
    - docker

dependencies:
  cache_directories:
    - ~/docker
  override:
    - if [[ -e ~/docker/image.tar ]]; then docker load -i ~/docker/image.tar; fi

    docker build -t testing -v $CIRCLE_ARTIFACTS:/verified .

    - mkdir -p ~/docker; docker save testing > ~/docker/image.tar
